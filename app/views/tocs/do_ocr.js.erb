$("#results").html("<pre><%= escape_javascript(@results) %></pre>");
$("#ocr_submit").prop('disabled', false);
$("#ocr_working").hide();
<% if @results.present? && !@results.start_with?('<') && !@results.start_with?('Error') %>
$("#paste_ocr").show();
<% end %>

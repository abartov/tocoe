# Enter your per-environment constants below

development: &default
  # OCR method: 'tesseract' for local binary or 'rest' for web service
  OCR_method: 'tesseract'
  # Path to tesseract binary (used when OCR_method is 'tesseract')
  tesseract_path: 'tesseract'
  # REST API endpoint for OCR service (used when OCR_method is 'rest')
  OCR_service: 'http://localhost:8080' # leave out the /ocr part
  # OCR language (default: eng for English)
  OCR_language: 'eng'
  # Google OAuth2 credentials (set via constants file per environment)
  google_oauth2_client_id: <replace-with-google-client-id>
  google_oauth2_client_secret: <replace-with-google-client-secret>
  # Gutendex API endpoint (Project Gutenberg catalog API)
  gutendex_api_url: 'https://gutendex.com'

production:
  <<: *default
  OCR_method: 'rest'

test:
  <<: *default
